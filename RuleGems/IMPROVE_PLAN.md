# RuleGems 改进实施计划

日期：2026-08-27。依据：[质量复审](docs/quality-review-2026-08-27.md)。
本文件按本次用户明确要求创建，仅记录本轮 RuleGems 的实施与验收；全仓路线仍以根目录 PLAN.md 为准。

## 边界与不变量

- 保留 Java 17 / Spigot API 1.16、EMBEDDED 打包、独立安装和现有可选依赖；不增加 Folia 支持承诺。
- 保留宝石 UUID、所有权、次数、委任/撤销 YAML 的既有路径与字段；旧文件可以直接读取。不重置存量额度。
- 保留主人/委任参数约束、补全、执行身份、默认禁用 transfer 与 OP 提权；不实现原命令补全继承。
- 普通命令的成功路径不变；本轮明确改变的是错误路径：不得把资金部分生效当作安全重试，不得把读取错误当作空配置，不得把保存失败当作成功。
- 通用文件安全能力放 cubex-config；RuleGems 的资金审计、委任和撤销状态仍留在插件内部。
- 不修改无关插件的工作区改动，不自动部署、提交或推送。每项先补针对性回归，再实施和验证。

## 执行顺序

| 项目 | 状态 | 实现与验收 |
|---|---|---|
| 1. 安全文件写入基础 | 完成 | AtomicYamlFiles / FileBackups 已加入 cubex-config；严格读、暂存校验、flush、替换和独立备份；模块测试通过 |
| 2. 转账结果与核账闭环 | 完成 | 明确未生效、成功、部分生效、待核账结果；转账命令执行前持久化操作标记、同步保存额度，结果不确定时冻结同一来源重试；提供带权限的人工核账确认命令，不自动补钱/退钱；覆盖两个资金复现及重载/重启保护 |
| 3. 门控与撤销数据安全 | 完成 | Rule/Revoke 先严格加载候选配置再发布；撤销冷却提取为 Reloadable/Terminable store，损坏读取不清空、不覆盖；纳入全局预校验；覆盖坏 YAML 与首次加载失败 |
| 4. 委任持久化与保存门禁 | 完成 | 抽离 AppointmentStore；写入成功后才发布委任/撤销变化，失败返回明确错误；脏数据未落盘不得从旧文件覆盖内存；旧格式/迁移保留；覆盖保存失败后重载与关闭 |
| 5. 生命周期与状态边界 | 完成 | 将重载编排提取为可测试组件，补保存失败、加载失败、关闭部分初始化资源等跨组件测试；把 RevokeFeature 对权限/额度 Map 的写入收口到领域方法；明确初始化顺序 |
| 6. 备份与历史查询 | 完成 | BackupHelper 复用不可覆盖备份，任一路径复制失败不报告完整成功；历史分页改成流式有界内存读取，保持顺序/筛选/计数；修正页码溢出并补测试 |
| 7. 质量与运维说明 | 完成 | 清理失效 Detekt 基线，不新增基线豁免、不降低门禁；新增核账命令执行/help/tab 权限一致；同步中英文 README、语言与升级/回滚说明；明确第三方热替换和实服性能边界 |
| 8. 完整验收 | 完成 | RuleGems clean build + jarGate、受影响共享模块/参考插件测试；复查产物隔离和升级数据；记录测试数、质量数据、JAR hash 与尚需实服验证的项目 |

## 关键设计

### 转账

操作记录保存在新增的 `data/transfer-operations.yml`，不改变现有宝石数据格式。
每次操作有 UUID、玩家、额度来源、命令标签、时间和结果。持久化开始标记失败时不触发任何转账。
有未完成或待核账记录时阻止同一玩家/来源再次执行；重启或 `/rg reload` 不会绕过该保护。

执行顺序为：验证参数/冷却 → 写操作标记 → 扣次数并同步保存 → 执行 → 根据结果保留/退回次数 → 同步保存 → 完成或保留待核账标记。
只在明确未产生副作用/已完整补偿时退次数；一旦资金已移动，后续命令失败也保留次数并进入冷却。
待核账必须人工确认；确认动作只解除保护并留记录，不自动重放命令或修改经济余额。
Vault 仍不提供跨账户原子事务，本轮不能宣称消除提供方内部故障或实现自动崩溃回滚。

### 功能数据

共享文件层负责严格 YAML、临时文件、校验、flush 和替换；业务 store 负责 schema、快照与脏状态。
配置读取失败保留运行中的旧配置；首次读取失败禁止保存空状态。
对于可以先持久化再发布的任命修改，写入失败时保持旧状态并返回失败；涉及外部权限/世界副作用的操作不假装能够跨插件原子回滚。
reload 必须先通过所有需要保存的 store 门禁，再进入加载链。

### 结构调整范围

本轮拆分实际触达的资金、功能存储和重载职责，封装撤销写状态入口；不为降低行数机械拆分所有大类。
不用全局覆盖率目标替代关键故障测试，不以“消除全部 Detekt 告警”作为此次完成条件。
第三方插件热替换、真实数据库/经济提供方断电测试和大服性能认证属于实服验证范围，本轮不冒充已验证。

## 验收清单

- [x] 转账到账后异常、后续命令失败不退额度；待核账无法重复执行。
- [x] 操作日志或额度保存失败时不会触发转账；重载/重启后仍能定位未完成操作。
- [x] 普通玩家不能查看/解除核账；确认操作不修改余额、不自动补次数。
- [x] 门控/撤销配置和数据损坏时，重载失败并保留旧状态、原文件。
- [x] 委任撤销保存失败不返回成功，之后重载不会出现“已成功撤销却恢复”。
- [x] 旧宝石/次数/委任/撤销数据可读，已有自定义配置不被替换。
- [x] 备份不互相覆盖，历史分页结果与既有行为一致。
- [x] 测试、静态分析、构建与 jar 门禁通过，所有残余运行环境验证明确列出。

## 执行记录

1. 共享文件基础：`.\gradlew.bat :modules:cubex-config:test --no-daemon` 通过；新增 5 个文件读写/访问失败/备份用例。保留原 YamlFiles 的兼容行为，新能力显式使用，不悄悄改变其他插件。
2. 转账闭环：原有监听器/资金回归及新核账权限测试通过（目标集 26 项）；新增操作日志与人工确认命令，已生效/不确定结果保留额度消耗，明确失败才退回。普通命令执行路径保持原行为。
3. 门控与撤销：RuleGateFeature、RevokeFeature、ConfigManagerReload 目标测试通过；新增损坏 YAML、错误布尔类型、首次加载失败、保存失败不撤权的回归。撤销冷却改为先写再生效，旧数据格式保持不变。
4. 委任数据：AppointmentStore 与现有委任/撤销命令目标测试通过；覆盖新增/撤销/开关写入失败、坏数据后关闭、旧路径迁移、未知字段与 UUID/时间戳保留。所有修改先写后发布，不再积累可被重载覆盖的未保存任命。
5. 生命周期与领域边界：重载协调器 5 个顺序/失败/互斥用例、部分初始化清理用例及撤销回归通过。Rule 门控和权限提供方在权限恢复之前就绪；RevokeFeature 不再直接写权限/额度 Map。
6. 历史与备份：两月排序/筛选/大页码、2 万行分页对照、UTF-8 写入，以及重复备份/中途复制失败用例通过；历史读取内存随页大小增长，不随整月文件增长。
7. 质量与说明：全部 RuleGems 与 cubex-config 测试已通过；核账执行/help/tab 权限一致，语言 v3 对已知旧默认文案做保守迁移。Detekt 当前清单 601 项，删除旧基线中 53 个失效项（654 → 601），没有新增豁免或放宽门禁。同步 README/README_en/CHANGELOG 与 MODULES 真实 API 示例。

8. 最终验收：干净构建、完整测试、Detekt 和 RuleGems/CubeXLib jarGate 通过。追加启动门控用例：先准备委任身份数据，再恢复依赖该身份的宝石权限。未部署、提交或推送。

## 最终验证记录

| 检查 | 结果 |
|---|---|
| RuleGems clean + build + jarGate | 通过；531 项测试，101 个测试类，失败/错误/跳过均为 0 |
| cubex-config test | 22 项通过 |
| Contract test（参考消费方） | 172 项通过 |
| CubeXLib jarGate | LIB 模式通过，共享字节码 major 61 |
| RuleGems Detekt | 通过；基线 654 → 601，仅删除 53 项失效记录，没有新增豁免 |
| RuleGems JaCoCo（排除 vendored Metrics） | 行 6068/12858 = 47.2%；分支 3282/9272 = 35.4% |
| 改进前同口径 | 490 项测试；行 43.6%，分支 32.9% |
| git diff --check（本轮涉及范围） | 通过 |

验证命令：

```powershell
.\gradlew.bat :RuleGems:clean :RuleGems:build :RuleGems:jarGate :modules:cubex-config:test :Contract:test :CubeXLib:jarGate --no-daemon --continue
# 最后增加委任门控启动顺序回归后，再次干净构建：
.\gradlew.bat :RuleGems:clean :RuleGems:build :RuleGems:jarGate --no-daemon
```

部署候选：`build/libs/RuleGems-1.1.0.jar`（8,246,208 字节；不是 plain.jar）。
SHA-256：`047864F09EA77B5BC33A2063B491A85083502C179F00773D2BD9C8857F5CE78A`。

产物检查：EMBEDDED，未重定位 Kotlin = 0，已重定位共享模块条目 = 161，插件及共享字节码 major = 61（Java 17）。
jar 内主类、核账命令、存储组件及共享文件类均存在；中英文语言版本为 3，没有新增 CubeXLib 强依赖或 Folia 支持声明。
代码版本仍为 1.1.0，本轮没有创建发布版本或标签。

## 升级与实服边界

- 停服备份旧 jar、完整 RuleGems 目录及外置 SQLite 文件，再替换部署候选；不删除配置或重新生成宝石。
- 启动后运行 `/rg doctor`，核对 UUID、归属、次数、委任和撤销冷却。对 `name:cubex_bank` 做小额实账验证后再开放转账。
- 有待核账操作时先核对双方余额和经济日志，再用 `/rg transfer-review resolve <UUID> <说明>` 记录确认。该命令不改余额、不补次数、不清冷却。
- 降级前必须处理待核账记录；旧版本不识别新增保护。恢复 RuleGems 文件不撤销经济插件已经执行的收支，需另行核对。
- 仍需实服验证：真实 Vault/权限后端、服务器版本安装矩阵、物理断电与磁盘故障、大服同步写入延迟和 TPS。未运行这些验证，也未运行全仓所有插件测试。
- 601 项遗留静态分析基线和部分编译警告仍在；这轮关闭了已确认故障并补上关键边界，不等于代码零债务或资金系统具备跨账户原子事务。全仓参考实现仍为 Contract。
