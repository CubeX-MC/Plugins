# —— Gradle ——
.gradle/
.gradle-verification/
**/build/
# 保留 wrapper jar(必须提交,否则别人无法用 ./gradlew 构建)
!gradle/wrapper/gradle-wrapper.jar

# —— run-paper 本地测试服残留 ——
**/run/

# —— 旧 Maven 产物(第1阶段迁移期间各插件可能仍有 target/)——
**/target/

# —— IDE ——
.idea/
.claude/
**/.claude/
*.iml
*.ipr
*.iws
.vscode/

# —— OS ——
.DS_Store
Thumbs.db

# —— 其它 ——
*.log
**/nul
**/dependency-reduced-pom.xml
lite-configs/
.agent/
